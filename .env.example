# ============================================
# UNISYNC ENVIRONMENT CONFIGURATION
# ============================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!
# ============================================

# Firebase Configuration
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id

# ============================================
# SECURITY CONFIGURATION
# ============================================

# reCAPTCHA v3 Site Key (for Firebase App Check)
# Get yours at: https://www.google.com/recaptcha/admin
# Enable App Check in Firebase Console first
VITE_RECAPTCHA_SITE_KEY=your_recaptcha_site_key

# ============================================
# PRODUCTION CHECKLIST
# ============================================
# Before deploying to production, ensure:
#
# 1. Firebase Console Settings:
#    - Enable App Check with reCAPTCHA
#    - Set authorized domains (Auth > Settings > Authorized domains)
#    - Enable only needed sign-in providers
#    - Set up email templates for password reset
#
# 2. Firestore:
#    - Deploy security rules (firebase deploy --only firestore:rules)
#    - Enable Firestore Audit Logging in GCP Console
#
# 3. Storage:
#    - Deploy security rules (firebase deploy --only storage)
#    - Set CORS configuration if needed
#
# 4. Authentication:
#    - Enable email verification
#    - Set password policy in Firebase Console
#    - Configure OAuth providers with proper credentials
#
# 5. General:
#    - Use HTTPS only
#    - Set Content Security Policy headers
#    - Enable Firebase Performance Monitoring
#    - Set up Firebase Crashlytics for error tracking
# ============================================
